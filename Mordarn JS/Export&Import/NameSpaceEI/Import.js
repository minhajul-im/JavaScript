// name space import
// it's work all export function in the export file, then you can get all function only dot notation.

import * as func from "./Export.js";

// summation
func.sum(10);

// subtraction
func.sub(10);

// multiplication
func.multi(10);
