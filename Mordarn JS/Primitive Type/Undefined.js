//start undefined

// 1.TODO: When a variable is declared without being assigned a value, its value is "undefined" by default.
let str;
console.log(str);

// 1.TODO: When an object property that does not exist is accessed, the returned value is "undefined".
const object = {};
console.log(object.name);
